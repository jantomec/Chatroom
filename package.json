{
  "name": "chatroom",
  "version": "0.0.0",
  "private": true,
  "description": "Terminal group chat for one user and two coding agents sharing one repository",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.9"
  },
  "dependencies": {
    "smol-toml": "^1.8.0"
  },
  "bin": {
    "chatroom": "bin/chatroom.js"
  }
}
